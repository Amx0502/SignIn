# 2026-09-17

## 移动端用户管理页样式迭代
- 全局 style.css:`.el-button.is-link { box-shadow:none !important }` 修复「编辑」链接按钮被 `.el-button--primary` 阴影波及的问题。
- UserTable.vue:移动端用户卡片紧凑化(dt/dd 四组字段 → 单行点号分隔摘要,角色并入标题行,按钮 36→30px);独立卡片 gap:14px、边框 #b6c6dc、头部浅灰蓝底 #eef2f8;按要求移除 header border-bottom 与 footer border-top。
- UserFilterBar.vue:删除 ≤420px 单列断点,移动端固定两列(搜索框跨整行)。

## 自动定位授权被 Chrome 拦截
- 现象为 Chrome 系统级安全机制(悬浮窗/滤镜覆盖时拒绝授权弹窗),非代码问题;geolocation.js PERMISSION_DENIED 文案改为可操作指引。

## 任务管理:项目列表选中反馈
- TaskManager.vue:新增 selectedProjectIndex,is-active 高亮(蓝边+左竖条+实心序号+Check 图标);重新获取列表/切换账号/重置/手动改序号时清除。

## 功能:系统账号导出 Excel + 卡片状态可视化(P0/P1 已实现)
- 背景:系统用户密码为 bcrypt 哈希不可逆,导出密码需保存副本。users 表新增 `initial_password_enc BLOB`,存 AES-GCM 加密副本(密钥 env EXPORT_SECRET_KEY 或 backend/export_secret.key 自动生成,已加 .gitignore);管理员创建/重置密码时写入,用户自己改密时清空。
- 后端:新增 password_vault.py(AES-GCM);auth_repository create_user/reset_password 写副本、change_password 清副本,新增 export_current_users;main.py 新增 `GET /api/users/export`(require_admin,openpyxl 生成 xlsx:用户名/密码/会员卡/功能范围/有效期/状态,审计日志写 logs/audit.log)。
- 前端:api/index.js 加 exportUsersApi(blob);UserManagement.vue 加「导出账号」按钮 + 会员卡类型下拉(全部/次卡/月卡),导出前确认明文密码风险。
- 可视化:新增 CardStatusBar.vue(月卡倒计时 >7 天绿/≤3 天橙/<24h 红+时间进度条;次卡剩余次数+额度进度条);UserTable.vue 桌面「到期时间」列与移动端卡片接入。
- 依赖:venv 安装 openpyxl,requirements.txt 加 openpyxl>=3.1.0,<4。测试新增 4 个用例,28 个全部通过。
- 注:存量用户无密码副本,导出显示「(未保存，重置密码后可导出)」;本机 PowerShell stdout 被吞,验证结果需文件中转。

## 修复:启动报 Unknown column 'users.initial_password_enc'
- 根因:`initial_password_enc` 被加进 version 1 迁移,但真实库 schema_migrations 已记录 v1~v3,旧版本迁移不会重跑,create_all 又不补列 → 1054。
- 修复:auth_migrations.py 新增 version 4 迁移 `add_initial_password_enc`(幂等检查后 ADD COLUMN BLOB NULL)。对本地 SignIn 库执行成功(v4 落库、列已补),30 个测试全过。
- 经验:改已有迁移版本的内容对已部署库无效,增量变更必须新增版本号。

## 修复:导出账号 500 + 新增用户端会员卡余量展示
- 导出 500 根因:main.py L897 审计日志写成 `admin.username`,而 require_admin 返回的是 dict → AttributeError。改为 `admin["username"]`。函数直调正常、线上 500 的反差就是因为它只在实际依赖注入路径触发。
- 会员卡展示:App.vue 移动端「更多功能→账户」区块新增「我的会员卡」,复用 user-management/CardStatusBar.vue(次卡剩余次数+进度条,月卡剩余天数+倒计时);新增 refreshCurrentUser() 调 POST /api/auth/verify(api/index.js 加 verifyTokenApi)刷新 currentUser 并同步 localStorage,onMounted 与打开弹层时触发;仅非管理员且 card_type 为 single/monthly 时显示。
- 验证:临时 2207 实例 + 真实 admin token 走 HTTP,导出返回 200 有效 xlsx(PK 头);verify 返回含 card_remaining_uses/card_status 等字段。

## 功能:位置填写项始终提交(不受检测结果影响)
- 需求:不管小小签到项目检测到什么填写项,位置(key 6)提交项一定要有。
- 前端 TaskManager.vue:新增常量 LOCATION_KEY=6 与 displayFillOptions(检测结果无位置时补一个合成「位置」标签);位置标签始终选中、点击提示"位置信息始终提交,无法取消";「签到位置」表单项改为恒显示,提示文案改为"位置信息始终随签到提交";检测成功后 locationMode none→auto;保存任务 fill_fields 强制加 "6"。
- 后端 service.py build_payload:force_location = fill_fields 为 None 或含 6;三条路径均兜底——全部取消时仍提交位置、无填写项定义旧路径在 use_location 或 force_location 时提交位置、检测项不含 6 时末尾强制 _append_location_param(required=False,无可用坐标时优雅跳过)。
- 测试:test_xxqd_ownership.py 新增 2 个用例(检测结果无位置仍提交位置/取消全部文字仅提交位置),全量 58 个通过;前端 vite build 通过。
- 注意:后端 run.py 无热重载,修复需用户手动重启后端进程。

## 用户管理页导出按钮改版
- UserManagement.vue:去掉独立「导出会员卡类型」下拉,导出按钮改为 el-dropdown,菜单项:全部账号/小小签到·次卡/小小签到·月卡/班级魔方·次卡/班级魔方·月卡(EXPORT_OPTIONS 映射 platform_scope+card_type,后端 /api/users/export 原生支持)。
- 移动端(≤640px)头部操作改为一行三个按钮(flex,导出 1.15 份宽,字号 13px),不再竖排三行。
- vite build 验证通过。注意 PowerShell Remove-Item 对部分文件失效时用 [System.IO.File]::Delete。

## 班级魔方账号页头部精简
- ClassCubeAccounts.vue:删除 el-card 的 #header(「账号管理」标题+「微信扫码添加账号」按钮)及 .page-head 样式。
- AccountCheckinPanel.vue:≤640px 时 .account-head-actions 由单列改为两列网格,批量删除/扫码添加一行显示(桌面端本就一行)。vite build 通过。

## 功能:用户备注(remark)
- 后端:users 表加 remark VARCHAR(255)(version 5 迁移,已对真实库执行);_to_dict/create_user/update_user 贯通 remark(normalize_remark:strip/空转 None/截 255);UserCreate/UserUpdate/PlatformMemberCreate 加 remark 字段;main.py 三个接口传参,update 用 model_fields_set 判断避免覆盖;导出 Excel 加「备注」列(G 列)。
- 前端:UserEditorDialog 加备注 textarea(255 字限制);MemberCardDialog(一键创建)加备注;UserTable 桌面加备注列、移动卡片加备注行(user-remark);userMembership createDefaultUserForm 加 remark:''。
- 测试:test_user_remark_create_update_and_export,29 个全过;vite build 通过。需重启后端生效。

## 导出菜单精简
- UserManagement.vue 导出下拉精简为三项:全部账号/小小签到/班级魔方(EXPORT_OPTIONS 仅按 platform_scope 筛选,去掉 card_type 拆分)。vite build 通过。

## 班级魔方运行记录:点击详情 + 系统账号展示
- 后端 class_cube_repository.list_runs:按 owner_user_id 批量查 users 表,记录附加 owner_username;owner_remark 仅 is_admin 时返回(测试独立库无 users 表时 try/except OperationalError 降级)。
- 前端 RunHistoryPanel.vue:run-row 可点击(与小小签到一致),新增详情抽屉(任务/平台账号/系统账号/账号备注(管理员)/状态/签到类型/起止时间/签到位置/结果说明/photo_res);列表 small 行追加「系统账号 xxx」;unknown_result 重试按钮加 @click.stop;移动端隐藏箭头。vite build + 11 个班级魔方测试通过。需重启后端生效。

## 修复:签到后「我的会员卡」不更新次数
- 排查:数据库实际已正确核销(bjmf 用户 card_used_count=1、card_used_at 落库、卡用完 30s 延迟后 is_active=0);问题纯前端——会员卡显示登录时快照,签到后无实时更新,且卡用完后 verify 401 静默失败继续显示旧数据。
- 修复:userMembership.js 新增 applyMembershipUpdate(payload)(兼容小小签到 membership_card 对象/班级魔方 membership_card_* 平铺字段),patch localStorage user 并广播 membership-updated 事件;useClassCube.manualCheckin 与 TaskManager.runTask 成功后调用;App.vue 监听事件更新 currentUser,CardStatusBar :key 加入 card_used_count/card_status 强制重渲染。vite build 通过。纯前端改动无需重启后端。

## 功能:用户详情抽屉 + 密码可复制
- 后端:auth_repository.get_user_credentials(user_id)(_to_dict + decrypt_password 解密初始密码副本);main.py 新增 GET /api/users/{user_id}/credentials(require_admin,写审计日志)。
- 前端:新建 UserInfoDrawer.vue(用户名/初始密码/角色/会员卡/功能范围/状态/有效期/登录/创建/备注,均有复制按钮,clipboard 兜底 execCommand);UserTable 桌面操作列加「详情」、移动端整卡点击开详情(footer @click.stop 防误触);UserManagement 接线。29 测试通过、vite build 通过。需重启后端。

## 功能:账号详情显示创建人 + 归档详情增强
- users 表新增 `created_by VARCHAR(50)`(version 6 独立迁移,已对真实库执行验证);create_user 增加 created_by 参数,main.py 两处创建接口(新增用户/一键创建)传 admin["username"];_to_dict 输出 created_by。
- GET /api/users/{id}/credentials 自然带 created_by;UserInfoDrawer.vue 新增「创建人」行(可复制,无则显示 —)。
- 新增测试 test_user_created_by_recorded_and_exposed;修复 repository.list_runs:owner 过滤原来仅非管理员生效,现管理员传 owner_user_id 也生效;/api/xxqd/runs 端点新增 owner_user_id 参数(非管理员强制自身 scope,管理员可指定)——之前归档跳转「查看小小签到记录」的过滤实际无效。
- ArchivedUserDetailDrawer.vue:新增「最近签到系统账号/平台账号」(按 platform_scope 拉 class-cube/xxqd runs limit=1,class-cube 用 owner_username,xxqd 回退 user.username)、创建人、备注行。
- 验证:43 个后端测试全过,vite build 通过。需重启后端生效。

## 桌面端顶栏会员卡徽标
- 新建 HeaderCardBadge.vue:顶栏用户名左侧胶囊徽标(仅非管理员且带卡用户可见),次卡「剩 N 次」/月卡「剩 N 天」倒计时(30s 刷新),绿/橙/红状态色;hover 弹出完整 CardStatusBar(与手机端「我的会员卡」一致)。App.vue 顶栏接入;移动端(≤768px)不显示(v-if="!isMobile"),手机端继续用「更多→我的会员卡」。vite build 通过,纯前端改动。

## 小任务:执行时间为空同时 message 提示
- TaskManager.saveTask 在 date_mode 校验后新增:form.times 为空时 validateField("times") 触发行内红字 + ElMessage.warning("请至少设置一个执行时间")。TaskManagement.vue 的 rules 为死代码(从未 validate),无需改。vite build 通过。

## UI:检测填写项按钮移位
- TaskManager.vue:「检测填写项」按钮从「任务设置」卡片头移到「签到项目列表」标题行右侧,与「获取项目列表」并排(section-header-actions flex 容器);未选项目时禁用。卡片头只留签到类型 tag。两处提示文案去掉「右上角」。vite build 通过。
- 「检测填写项」按钮样式统一为与「获取项目列表」一致:primary plain + DocumentChecked 图标 + size=small,检测中/未选项目时禁用。vite build 通过。

## 功能:检测填写项可勾选提交(fill_fields)
- 背景:项目21接口确实返回文字 status=1/require=1,但用户实际不需要;方案=用户可取消勾选检测到的填写项,取消后保存任务不提交该项。
- 后端:tasks 表新增 fill_fields JSON NULL 列(database.py 迁移+db_models);repository/_normalize_fill_fields、_task_to_dict、_apply_task;models.TaskCreate.fill_fields;service.normalize_fill_fields+normalize_task;build_payload:fill_fields 非空列表时过滤 fill_options,全取消则直接普通签到提交,None 保持原行为。
- 前端 TaskManager.vue:检测标签可点击切换勾选(选中彩色/取消灰色 plain+提示文字),hasText/Image/LocationFill 与 customFillItems 按 activeFillOptions 派生;saveTask 提交 fill_fields;地图位置校验仅在勾选位置项时生效;检测 toast 文案改为「检测到填写项…(不需要的可点击标签取消)」;编辑任务时沿用已保存勾选。
- 测试:test_build_payload_fill_fields_deselect_skips_item,33 个全过;真实库 fill_fields 列迁移已执行;vite build 通过。需重启后端。
